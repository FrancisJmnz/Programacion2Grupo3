@model IEnumerable<Venta.Web.Models.MenuModelo>

@{
    ViewData["Title"] = "Index";
}

<h1>Menu<</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.icono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idMenuRol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.icono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idMenuRol)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idMenu}) |
                @Html.ActionLink("Details", "Details", new { id=item.idMenu}) |
            </td>
        </tr>
}
    </tbody>
</table>
